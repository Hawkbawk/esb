{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hawkbawk.github.io/esb/esb.schema.json",
  "properties": {
    "kits": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "dockerfile": {
      "type": "string"
    },
    "setupScript": {
      "type": "string"
    },
    "$schema": {
      "type": "string",
      "description": "Path or URL to this JSON schema."
    }
  },
  "additionalProperties": false,
  "type": "object",
  "title": ".esb.json",
  "description": "Per-repo config for `esb from-template`, read from .esb.json at the repo root."
}
